export default function Services() {
  // Services are represented directly in the Hero panel grid.
  // This section is intentionally minimal — the homepage panels serve as the primary services display.
  return null;
}
